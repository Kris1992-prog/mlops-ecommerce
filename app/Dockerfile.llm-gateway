FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn httpx pydantic

COPY llm_gateway.py .

EXPOSE 8001

CMD ["uvicorn", "llm_gateway:app", "--host", "0.0.0.0", "--port", "8001"]
